<UserControl x:Class="UniLineGo.Presentation.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#EDE8ED">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="460"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="1" VerticalAlignment="Center"
                Background="#FFFFFF" CornerRadius="16"
                Padding="48,44" Margin="0,40">
            <Border.Effect>
                <DropShadowEffect BlurRadius="24" ShadowDepth="0"
                                  Opacity="0.15" Color="#C3B3D4"/>
            </Border.Effect>

            <StackPanel>

                <!-- Title -->
                <TextBlock Text="Мій профіль"
                           FontSize="28"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Foreground="#C3B3D4"
                           Margin="0,0,0,32"/>

                <!-- Username -->
                <TextBlock Text="Ім'я користувача"
                           Foreground="#C3B3D4"
                           FontWeight="SemiBold"
                           Margin="0,0,0,6"/>

                <TextBox x:Name="UsernameBox"
                         Height="44"
                         Padding="14,10"
                         Background="#EDE8ED"
                         BorderBrush="#DACADD"
                         Foreground="#5A4E6D"
                         FontFamily="Segoe UI"
                         FontSize="14"    
                         BorderThickness="1.5"/>

                <!-- Email -->
                <TextBlock Text="Email"
                           Foreground="#C3B3D4"
                           FontWeight="SemiBold"
                           Margin="0,14,0,6"/>

                <TextBox x:Name="EmailBox"
                         Height="44"
                         Padding="14,10"
                         Background="#EDE8ED"
                         BorderBrush="#DACADD"
                         Foreground="#5A4E6D"
                         FontFamily="Segoe UI"
                         FontSize="14"
                         BorderThickness="1.5"/>

                <!-- New Password -->
                <TextBlock Text="Новий пароль"
                           Foreground="#C3B3D4"
                           FontWeight="SemiBold"
                           Margin="0,14,0,6"/>

                <PasswordBox x:Name="NewPasswordBox"
                             Height="44"
                             Padding="14,10"
                             Background="#EDE8ED"
                             BorderBrush="#DACADD"
                             Foreground="#C3B3D4"
                             FontFamily="Segoe UI"
                             FontSize="14"
                             BorderThickness="1.5"/>

                <!-- Confirm -->
                <TextBlock Text="Підтвердіть новий пароль"
                           Foreground="#C3B3D4"
                           FontWeight="SemiBold"
                           Margin="0,14,0,6"/>

                <PasswordBox x:Name="ConfirmPasswordBox"
                             Height="44"
                             Padding="14,10"
                             Background="#EDE8ED"
                             BorderBrush="#DACADD"
                             Foreground="#C3B3D4"
                             FontFamily="Segoe UI"
                             FontSize="14"
                             BorderThickness="1.5"/>

                <!-- Message -->
                <TextBlock x:Name="MessageText"
                           FontSize="12"
                           Margin="0,10,0,0"
                           Visibility="Collapsed"
                           Foreground="#C3B3D4"/>

                <!-- Save -->
                <Button Content="Зберегти зміни"
                        Height="46"
                        Margin="0,24,0,0"
                        FontSize="15"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="SaveButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="#C3B3D4"
                                                CornerRadius="8">
                                            <ContentPresenter
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                TextElement.Foreground="White"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Back -->
                <Button Content="← Назад"
                        Height="36"
                        Margin="0,8,0,0"
                        Background="Transparent"
                        Foreground="#DACADD"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="BackButton_Click"/>

                <Separator Background="#DACADD" Margin="0,20" Opacity="0.4"/>

                <!-- Logout -->
                <Button Content="Вийти з акаунту"
                        Height="46"
                        FontSize="15"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="LogoutButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="#FAEECD"
                                                CornerRadius="8">
                                            <ContentPresenter
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                TextElement.Foreground="#C3B3D4"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>