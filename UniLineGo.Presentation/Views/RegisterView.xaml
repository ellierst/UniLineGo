<UserControl x:Class="UniLineGo.Presentation.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#EBDEC0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="420"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="1" VerticalAlignment="Center"
                Background="#FAF7F0" CornerRadius="16"
                Padding="48,44" Margin="0,40">
            <Border.Effect>
                <DropShadowEffect BlurRadius="24" ShadowDepth="0" Opacity="0.15" Color="#768E78"/>
            </Border.Effect>
            <StackPanel>

                <TextBlock Text="UniLineGo" FontSize="34" FontWeight="Bold"
                           HorizontalAlignment="Center" Foreground="#768E78"
                           FontFamily="Segoe UI" Margin="0,0,0,6"/>
                <TextBlock Text="Створіть новий акаунт" FontSize="14"
                           HorizontalAlignment="Center" Foreground="#C6C09C"
                           FontFamily="Segoe UI" Margin="0,0,0,36"/>

                <TextBlock Text="Ім'я користувача" FontSize="13" Foreground="#768E78"
                           FontFamily="Segoe UI" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <TextBox x:Name="UsernameBox" Height="44" Padding="14,10"
                         FontSize="14" FontFamily="Segoe UI"
                         BorderBrush="#C6C09C" BorderThickness="1.5" Background="#FDFBF7"/>

                <TextBlock Text="Email" FontSize="13" Foreground="#768E78"
                           FontFamily="Segoe UI" FontWeight="SemiBold" Margin="0,14,0,6"/>
                <TextBox x:Name="EmailBox" Height="44" Padding="14,10"
                         FontSize="14" FontFamily="Segoe UI"
                         BorderBrush="#C6C09C" BorderThickness="1.5" Background="#FDFBF7"/>

                <TextBlock Text="Пароль" FontSize="13" Foreground="#768E78"
                           FontFamily="Segoe UI" FontWeight="SemiBold" Margin="0,14,0,6"/>
                <PasswordBox x:Name="PasswordBox" Height="44" Padding="14,10"
                             FontSize="14" FontFamily="Segoe UI"
                             BorderBrush="#C6C09C" BorderThickness="1.5" Background="#FDFBF7"/>

                <TextBlock Text="Підтвердіть пароль" FontSize="13" Foreground="#768E78"
                           FontFamily="Segoe UI" FontWeight="SemiBold" Margin="0,14,0,6"/>
                <PasswordBox x:Name="ConfirmPasswordBox" Height="44" Padding="14,10"
                             FontSize="14" FontFamily="Segoe UI"
                             BorderBrush="#C6C09C" BorderThickness="1.5" Background="#FDFBF7"/>

                <TextBlock x:Name="ErrorText" Foreground="#E79897" FontSize="12"
                           FontFamily="Segoe UI" Margin="0,10,0,0"
                           TextWrapping="Wrap" Visibility="Collapsed"/>

                <Button Content="Зареєструватись" Height="46" Margin="0,24,0,0"
                        FontSize="15" FontFamily="Segoe UI" FontWeight="SemiBold"
                        BorderThickness="0" Cursor="Hand" Click="RegisterButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="#768E78" CornerRadius="8" Padding="0,12">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"
                                                              TextElement.Foreground="White"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <TextBlock Text="Вже є акаунт? " Foreground="#C6C09C"
                               FontSize="13" FontFamily="Segoe UI"/>
                    <TextBlock Text="Увійти" Foreground="#E79897"
                               FontSize="13" FontFamily="Segoe UI"
                               Cursor="Hand" TextDecorations="Underline"
                               MouseLeftButtonUp="GoToLogin_Click"/>
                </StackPanel>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>