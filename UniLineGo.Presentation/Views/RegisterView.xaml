<UserControl x:Class="UniLineGo.Presentation.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#EDE8ED">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="420"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="1" VerticalAlignment="Center"
                Background="#FFFFFF" CornerRadius="16"
                Padding="48,44" Margin="0,40">
            <Border.Effect>
                <DropShadowEffect BlurRadius="24" ShadowDepth="0"
                                  Opacity="0.15" Color="#C3B3D4"/>
            </Border.Effect>

            <StackPanel>

                <!-- Title -->
                <TextBlock Text="UniLineGo"
                           FontSize="34"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Foreground="#C3B3D4"
                           Margin="0,0,0,6"/>

                <TextBlock Text="Створіть новий акаунт"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Foreground="#DACADD"
                           Margin="0,0,0,36"/>

                <!-- Username -->
                <TextBlock Text="Ім'я користувача"
                           Foreground="#C3B3D4"
                           FontWeight="SemiBold"
                           Margin="0,0,0,6"/>

                <TextBox x:Name="UsernameBox"
                         Height="44"
                         Padding="14,10"
                         Background="#EDE8ED"
                         BorderBrush="#DACADD"
                         Foreground="#5A4E6D"
                         FontFamily="Segoe UI"
                         FontSize="14"
                         BorderThickness="1.5"/>

                <!-- Email -->
                <TextBlock Text="Email"
                           Foreground="#C3B3D4"
                           FontWeight="SemiBold"
                           Margin="0,14,0,6"/>

                <TextBox x:Name="EmailBox"
                         Height="44"
                         Padding="14,10"
                         Background="#EDE8ED"
                         BorderBrush="#DACADD"
                         Foreground="#5A4E6D"
                         FontFamily="Segoe UI"
                         FontSize="14"
                         BorderThickness="1.5"/>

                <!-- Password -->
                <TextBlock Text="Пароль"
                           Foreground="#C3B3D4"
                           FontWeight="SemiBold"
                           Margin="0,14,0,6"/>

                <PasswordBox x:Name="PasswordBox"
                             Height="44"
                             Padding="14,10"
                             Background="#EDE8ED"
                             BorderBrush="#DACADD"
                             Foreground="#C3B3D4"
                             FontFamily="Segoe UI"
                             FontSize="14"
                             BorderThickness="1.5"/>

                <!-- Confirm -->
                <TextBlock Text="Підтвердіть пароль"
                           Foreground="#C3B3D4"
                           FontWeight="SemiBold"
                           Margin="0,14,0,6"/>

                <PasswordBox x:Name="ConfirmPasswordBox"
                             Height="44"
                             Padding="14,10"
                             Background="#EDE8ED"
                             BorderBrush="#DACADD"
                             Foreground="#C3B3D4"
                             FontFamily="Segoe UI"
                             FontSize="14"
                             BorderThickness="1.5"/>

                <!-- Error -->
                <TextBlock x:Name="ErrorText"
                           Foreground="#C3B3D4"
                           FontSize="12"
                           Margin="0,10,0,0"
                           Visibility="Collapsed"/>

                <!-- Button -->
                <Button Content="Зареєструватись"
                        Height="46"
                        Margin="0,24,0,0"
                        FontSize="15"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="RegisterButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="Bg"
                                                Background="#C3B3D4"
                                                CornerRadius="8">
                                            <ContentPresenter
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                TextElement.Foreground="White"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bg" Property="Background" Value="#b3a3c4"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Login link -->
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0,20,0,0">

                    <TextBlock Text="Вже є акаунт? "
                               Foreground="#DACADD"/>

                    <TextBlock Text="Увійти"
                               Foreground="#C3B3D4"
                               Cursor="Hand"
                               TextDecorations="Underline"
                               MouseLeftButtonUp="GoToLogin_Click"/>
                </StackPanel>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>