<UserControl x:Class="UniLineGo.Presentation.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#EDE8ED">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="420"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="1" VerticalAlignment="Center"
                Background="#FFFFFF" CornerRadius="16"
                Padding="48,44" Margin="0,40">
            <Border.Effect>
                <DropShadowEffect BlurRadius="24" ShadowDepth="0"
                                  Opacity="0.15" Color="#C3B3D4"/>
            </Border.Effect>

            <StackPanel>

                <!-- Title -->
                <TextBlock Text="UniLineGo"
                           FontSize="34"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Foreground="#C3B3D4"
                           Margin="0,0,0,6"/>

                <TextBlock Text="Увійдіть до свого акаунту"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Foreground="#DACADD"
                           Margin="0,0,0,36"/>

                <!-- Email -->
                <TextBlock Text="Email"
                           Foreground="#C3B3D4"
                           FontWeight="SemiBold"
                           Margin="0,0,0,6"/>

                <TextBox x:Name="EmailBox"
                         Height="44"
                         Padding="14,10"
                         Background="#EDE8ED"
                         BorderBrush="#DACADD"
                         Foreground="#5A4E6D"
                         FontFamily="Segoe UI"
                         FontSize="14"
                         BorderThickness="1.5"/>

                <!-- Password -->
                <TextBlock Text="Пароль"
                           Foreground="#C3B3D4"
                           FontWeight="SemiBold"
                           Margin="0,16,0,6"/>

                <PasswordBox x:Name="PasswordBox"
                             Height="44"
                             Padding="14,10"
                             Background="#EDE8ED"
                             BorderBrush="#DACADD"
                             Foreground="#C3B3D4"
                             FontFamily="Segoe UI"
                             FontSize="14"
                             BorderThickness="1.5"/>

                <!-- Error -->
                <TextBlock x:Name="ErrorText"
                           Foreground="#C3B3D4"
                           FontSize="12"
                           Margin="0,10,0,0"
                           Visibility="Collapsed"/>

                <!-- Login Button -->
                <Button Content="Увійти"
                        Height="46"
                        Margin="0,24,0,0"
                        FontSize="15"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="LoginButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="Bg"
                                                Background="#C3B3D4"
                                                CornerRadius="8">
                                            <ContentPresenter
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                TextElement.Foreground="White"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bg" Property="Background" Value="#b3a3c4"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Register link -->
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0,20,0,0">

                    <TextBlock Text="Немає акаунту? "
                               Foreground="#DACADD"/>

                    <TextBlock Text="Зареєструватись"
                               Foreground="#C3B3D4"
                               Cursor="Hand"
                               TextDecorations="Underline"
                               MouseLeftButtonUp="GoToRegister_Click"/>
                </StackPanel>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>