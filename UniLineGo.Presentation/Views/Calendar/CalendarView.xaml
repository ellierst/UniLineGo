<UserControl x:Class="UniLineGo.Presentation.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="DayHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#9A8A9E"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#EDE8ED"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F5F2F0" Margin="32,28,32,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="Календар"
                       FontSize="28" FontWeight="Bold"
                       Foreground="#C3B3D4"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    Height="40" Padding="18,0"
                    FontSize="13" FontWeight="SemiBold"
                    Cursor="Hand"
                    Click="AddEventButton_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="#C3B3D4" CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="+ " FontSize="15" FontWeight="Bold"
                                           Foreground="White" VerticalAlignment="Center"/>
                                <TextBlock Text="Додати завдання" Foreground="White"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#b3a3c4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal"
                    Margin="0,0,0,12" VerticalAlignment="Center">
            <Button Style="{StaticResource NavButtonStyle}" Click="PrevMonth_Click">
                <TextBlock Text="‹" FontSize="22" FontWeight="Bold"
                           Foreground="#C3B3D4"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Button>

            <TextBlock x:Name="MonthLabel"
                       FontSize="16" FontWeight="SemiBold"
                       Foreground="#C3B3D4"
                       VerticalAlignment="Center"
                       Margin="10,0" MinWidth="180"/>

            <Button Style="{StaticResource NavButtonStyle}" Click="NextMonth_Click">
                <TextBlock Text="›" FontSize="22" FontWeight="Bold"
                           Foreground="#C3B3D4"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Button>
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Пн" Style="{StaticResource DayHeaderStyle}"/>
            <TextBlock Grid.Column="1" Text="Вт" Style="{StaticResource DayHeaderStyle}"/>
            <TextBlock Grid.Column="2" Text="Ср" Style="{StaticResource DayHeaderStyle}"/>
            <TextBlock Grid.Column="3" Text="Чт" Style="{StaticResource DayHeaderStyle}"/>
            <TextBlock Grid.Column="4" Text="Пт" Style="{StaticResource DayHeaderStyle}"/>
            <TextBlock Grid.Column="5" Text="Сб" Style="{StaticResource DayHeaderStyle}"/>
            <TextBlock Grid.Column="6" Text="Нд" Style="{StaticResource DayHeaderStyle}"/>
        </Grid>

        <Border Grid.Row="3" Background="White" CornerRadius="12" Margin="0,6,0,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.06" Color="#6B5B7B"/>
            </Border.Effect>
            <Grid x:Name="CalendarGrid" ClipToBounds="True"/>
        </Border>

    </Grid>
</UserControl>